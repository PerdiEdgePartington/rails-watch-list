<h2><%= @list.name %></h2>

<p><%= link_to "add a bookmark", "/lists/#{@list.id}/bookmarks/new" %><p>

<p><strong>Bookmarks</strong></p>
<ul class="list-group">
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item"><%= bookmark.comment %></li>
  <% end %>
</ul>
<br>

<p><%= link_to "back to index", "/lists" %><p>
